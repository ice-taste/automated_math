#calculates area of triangle based on inches squared

def areat():
    base = float(input("Base: "))
    height = float(input("Height: "))
    area = (1 / 2) * base * height
    print(f"Area: {area:.3f}")

areat()
