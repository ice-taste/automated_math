#converts celsius to fahrenheit or fahrenheit to celsius


conversion = input('Enter C for Celsius or F for Fahrenheit: ').strip().upper()
temp = float(input('Enter temperature: '))

if conversion == 'C':
    fahrenheit = (temp * (9/5)) + 32.0
    print(f'Fahrenheit: {fahrenheit:.2f}')
elif conversion == 'F':
    celsius = (temp - 32.0) / (9/5)
    print(f'Celsius: {celsius:.2f}')
else:
    print('Invalid input, please enter a valid command')
